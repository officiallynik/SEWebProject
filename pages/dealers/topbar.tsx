import React from 'react';
import styles from '../../styles/Dealers.module.css';

const DealerTopBar = (props) => {
    return (
        <div className={styles.topbar}>
            
        </div>
    );
};

export default DealerTopBar;